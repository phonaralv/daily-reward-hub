// PR-1 stub: hooks into SW `waiting` event when vite-plugin-pwa is wired (PR-2).
// For now exposes a no-op so call sites can compile.
export function useUpdateToast(): void {
  // intentionally empty in PR-1
}
